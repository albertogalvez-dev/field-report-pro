pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "FieldReportPro"
include(":app")

def buildRoot = new File(System.getProperty("user.home"), ".fieldreportpro-build")
gradle.beforeProject { project ->
    project.buildDir = new File(buildRoot, project.name)
}
