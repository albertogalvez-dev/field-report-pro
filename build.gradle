plugins {
    id 'com.android.application' version '8.2.2' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.24' apply false
    id 'app.cash.paparazzi' version '1.3.4' apply false
}

tasks.register("ci") {
    group = "verification"
    description = "Assemble debug, run unit tests, and verify Paparazzi snapshots."
    dependsOn(":app:assembleDebug", ":app:testDebugUnitTest", ":app:verifyPaparazziDebug")
}
